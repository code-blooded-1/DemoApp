<%= form_with(model: pan_detail) do |form| %>
  <% if pan_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pan_detail.errors.count, "error") %> prohibited this pan_detail from being saved:</h2>

      <ul>
        <% pan_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :number, style: "display: block" %>
    <%= form.text_field :number %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
